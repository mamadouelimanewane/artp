{
  "version": 2,
  "buildCommand": "npm run build -w packages/web-admin",
  "outputDirectory": "packages/web-admin/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ]
}
